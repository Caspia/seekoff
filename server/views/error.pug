extends layout

block content
  .container
    .row
      .col-md-9
        h2.questiontitle= message
        if errors
          ul
            for error in errors
              li= error.msg || error
      .col-md-3
