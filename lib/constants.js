/**
 * Constants used in various places in the app.
 */

module.exports = {
  INDEX_PREFIX: 'stackoverflow_',
  DEFAULT_WEB_PORT: 8080,
  // How many active lines are allowed in reading from a file?
  MAX_READ_LINES: 256,
  // Timeout for single document index in milliseconds
  INDEX_TIMEOUT: 10000,
};
